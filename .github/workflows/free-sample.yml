name: Free Sample Report

on:
  schedule:
    - cron: "0 */12 * * *"   # updates every 12 hours
  workflow_dispatch:

jobs:
  free-sample:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install report deps
        run: pip install -r requirements-reports.txt

      - name: Generate free sample
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python -m trenddrop.reports.free_sample

      # Upload disabled because Supabase CLI is not available in GitHub Actions
      # - name: Upload to Supabase Storage
      #   run: |
      #     supabase storage upload trenddrop-reports/free/sample.pdf out/free_sample.pdf --replace
      #     supabase storage upload trenddrop-reports/free/sample.csv out/free_sample.csv --replace
